<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bawei.shaopenglai">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".application.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ShoppingTrolleyActivity" />
        <activity android:name=".ui.mineui.PersonalInforActivity" />
        <activity android:name=".ui.mineui.MyGroupActivity" />
        <activity android:name=".ui.mineui.CityListActivity" />
        <activity android:name=".ui.shopping.TJDDActivity" />
        <activity android:name=".ui.GoodsActivity" />
        <activity android:name=".ui.mineui.AddAddressActivity" />
        <activity android:name=".ui.mineui.MineMoneyActivity" />
        <activity android:name=".ui.mineui.MainFootActivity" />
        <activity android:name=".fragment.lb.SecondActivity"></activity>
    </application>

</manifest>